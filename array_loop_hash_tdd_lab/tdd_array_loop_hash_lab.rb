# add your code to be tested here


def array_length(array1, array2)
  return (array1 + array2).length
end


def array_sum_total(array) 
  total = 0
    for i in array
      total += i
    end
  return total
end

def find_item(item)
  array.include?(item)
end